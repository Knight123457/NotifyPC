# NotifyPC 使用说明

手机通知 → 电脑看板。手机装 APK，电脑双击 exe，浏览器看实时通知与汇总待办。

**v2.5**：支持 **WiFi** 与 **蓝牙** 两种传输；断线重连、本地缓存、看板 Token 访问控制。

## 一、交付清单

| 文件 | 用途 | 必选 |
|------|------|------|
| `NotifyPC.apk` | 手机端（v2.5：WiFi / 蓝牙双模式） | ✅ |
| `skill/notify-dashboard/` | WorkBuddy 技能（**自包含**，电脑端桥接 exe / 看板页 / 部署脚本全在里面，见第二节） | ✅ |

> 电脑端不再单独发放 exe——它已收进 `skill/notify-dashboard/bin/notify-bridge.exe`，由技能自动部署。非 WorkBuddy 用户也可手动从该路径取出 exe 使用（见 2.3）。

## 二、电脑端部署

### 2.1 WorkBuddy 用户（推荐，自动）

1. 把 `skill/notify-dashboard` 整个文件夹复制到 `%USERPROFILE%\.workbuddy\skills\`（即 `C:\Users\你的用户名\.workbuddy\skills\`）
2. 对话说「**通知看板**」→ 自动完成：部署 exe → 放行防火墙 → 启动服务 → 打开看板，全程无需关心文件位置
3. （可选）想开机自动常驻：让 WorkBuddy 运行一次技能里的 `install.ps1 -AutoStart`，之后电脑开机桥接自动运行

> 数据与技能分离：通知日志和配置写在 `%USERPROFILE%\NotifyPC\`（exe 运行目录），删除/更新技能不影响历史数据。

### 2.2 首次启动防火墙

无论是自动还是手动部署，首次启动 `notify-bridge.exe` 时 Windows 防火墙弹窗请点「**允许访问**」，否则手机永远连不上（最常见的失败原因）。

### 2.3 非 WorkBuddy 用户（手动）

1. 从 `skill/notify-dashboard/bin/` 把 `notify-bridge.exe` 复制到固定目录（推荐 `C:\Users\你的用户名\NotifyPC\`）
2. 双击 exe 运行
3. 浏览器打开 `http://localhost:9875`，看到看板即成功

> **看板访问控制**：exe 同目录 `config.json` 里的 `dashToken`  
> - 本机 `http://localhost:9875` 默认免密  
> - 局域网需用日志里的 `http://电脑IP:9875/?token=口令`  
> - 仅本机可打开看板：`"dashBind": "127.0.0.1"`

启动日志里应能看到类似：

- `TCP/WiFi : ... 9876`
- `蓝牙 RFCOMM : 已监听`（电脑蓝牙已开时）

技能是**自包含**的——桥接 exe、看板页、部署/启动脚本全部打包在技能目录里，**不需要手动放置任何文件**：

```
skill/notify-dashboard/
├── SKILL.md                ← 技能剧本
├── bin/notify-bridge.exe   ← 桥接程序（部署源）
├── dashboard.html          ← 看板页（可选，覆盖 exe 内嵌版）
├── config.example.json     ← 配置模板
└── scripts/
    ├── install.ps1         ← 一键部署：复制 exe → %USERPROFILE%\NotifyPC + 防火墙 + 可选自启
    └── start.ps1           ← 启动器：确保已部署 → 启动 → 等待就绪
```

1. 把 `skill/notify-dashboard` 整个文件夹复制到 `%USERPROFILE%\.workbuddy\skills\`（即 `C:\Users\你的用户名\.workbuddy\skills\`）
2. 对话说「**通知看板**」→ 自动完成：部署 exe → 放行防火墙 → 启动服务 → 打开看板，全程无需关心文件位置
3. （可选）想开机自动常驻：让 WorkBuddy 运行一次技能里的 `install.ps1 -AutoStart`，之后电脑开机桥接自动运行

> 数据与技能分离：通知日志和配置写在 `%USERPROFILE%\NotifyPC\`（exe 运行目录），删除/更新技能不影响历史数据。

## 四、手机端：WiFi 模式（默认）

1. 安装 `NotifyPC.apk`（确认版本 **v2.5.0** 及以上）  
2. 手机与电脑连**同一 WiFi**  
3. 打开 App，选 **WiFi**  
4. 填电脑局域网 IP，端口 **9876**（看板顶部会显示「手机填 x.x.x.x:9876」）  
5. 打开「通知使用权」、按机型做完省电/自启动  
6. 点 **保存并开始转发** → **发送一条测试消息**  

## 五、手机端：蓝牙模式

适合锁屏后 WiFi 易断、或暂时不在同一局域网的场景（仍需系统不把 App 冻死）。

### 5.1 电脑侧

1. 打开电脑 **蓝牙**，确保可被发现 / 允许配对  
2. 启动 `notify-bridge.exe`，日志出现 **「蓝牙 RFCOMM : 已监听」**  
3. （可选）在 Windows「设置 → 蓝牙」里确认本机蓝牙名称，方便手机辨认  

### 5.2 手机侧

1. **系统设置**里与电脑完成蓝牙配对（不要只开蓝牙不配对）  
2. 打开 NotifyPC，顶部选 **蓝牙**  
3. 点 **刷新已配对设备**，在列表里选中电脑  
4. 允许 App 的蓝牙权限（Android 12+ 会弹窗）  
5. 照常打开通知使用权、省电无限制等  
6. 点 **保存并开始转发** → **发送一条测试消息**  
7. 电脑看板出现「蓝牙模式测试通知」→ 成功  

### 5.3 注意

| 点 | 说明 |
|----|------|
| 协议 | 与 WiFi 相同（一行 JSON）；看板不用改 |
| 信道 | RFCOMM **信道 5**（App 与桥接已写死对齐） |
| 切换模式 | 改 WiFi/蓝牙后需再点一次「保存并开始转发」 |
| 能力边界 | 蓝牙主要减轻「锁屏断 WiFi」；**解决不了**厂商杀后台、锁屏通知脱敏 |
| 配对失败 | 先忘掉设备重新配对；确认桥接日志里蓝牙已监听；电脑蓝牙驱动正常 |

## 六、联调验证（通用）

1. 桥接在跑，看板能打开  
2. App 状态为「转发=运行中」，且为「已连接 / 蓝牙已连接」  
3. 点测试消息，或让微信/QQ 来一条  
4. 看板约 3 秒内出现  

## 七、常见问题

| 问题 | 原因与解决 |
|------|-----------|
| WiFi：看板没反应 | IP 是否为本机局域网 IP；同一 WiFi；防火墙放行 9876 |
| 蓝牙：连不上 / 一直重连 | ① 系统里是否已配对；② 桥接是否显示 RFCOMM 已监听；③ 选对电脑设备；④ 重新配对后再「保存并开始转发」 |
| 蓝牙列表是空的 | 先到系统蓝牙里配对电脑，再点「刷新已配对设备」；检查蓝牙权限 |
| 看板打不开 | exe 是否在跑；`netstat` 看 9875/9876 |
| 锁屏后收不到 | 省电无限制 + 自启动；可改试蓝牙模式；脱敏时解锁后才有完整正文属系统限制 |
| 局域网别人也能打开看板 | 用带 `?token=` 的链接，或 `dashBind=127.0.0.1` |
| 想换端口 / 口令 | exe 同目录 `config.json` |

## 八、日志与排障

- 通知日志：exe 同目录 `logs\notify.jsonl`  
- 端口：`netstat -ano | findstr "9876 9875"`  
- 重启：任务管理器结束 `notify-bridge.exe`，WorkBuddy 用户再说一次「通知看板」即自动拉起（或手动双击 exe）  
