# NotifyPC 使用说明 v4.0

手机通知 → 电脑看板。**BLE 统一传输**：Android（App + GATT）/ iPhone（ANCS，无需 App）。

## 一、交付清单

| 文件 | 用途 |
|------|------|
| `NotifyPC.apk` | Android v4.0+ |
| `notify-bridge.exe` | 电脑桥接（或 skill 自动下载） |
| `skill/notify-dashboard/` | WorkBuddy 技能 |

## 二、电脑端

1. 运行 `notify-bridge.exe`（或 WorkBuddy 说「通知看板」）
2. 确保 **电脑蓝牙已开启**
3. 浏览器打开 `http://localhost:9875`
4. 日志应出现：`BLE Android : 已启动`、`BLE iOS/ANCS: 已启动`

配置目录：`%USERPROFILE%\NotifyPC\`

```json
{
  "dashPort": 9875,
  "ancsEnabled": true,
  "iosBleAddress": ""
}
```

- `iosBleAddress`：留空自动扫描名称含 iPhone 的设备；也可填蓝牙地址如 `AA:BB:CC:DD:EE:FF`

## 三、Android 手机

1. 安装 v4.0 APK
2. 打开「通知使用权」
3. 点 **保存并开始转发**
4. 状态应显示「BLE 广播中，等待电脑连接…」→「BLE 已连接」
5. 可点「发送测试消息」验证

**无需**再选择电脑 MAC 或填 IP（已移除 WiFi / RFCOMM）。

## 四、iPhone

1. 与电脑完成蓝牙配对
2. 设置 → 蓝牙 → 点你的电脑 → 打开 **共享系统通知**
3. 用「提醒事项」或短信测试
4. 看板通知带 `src: ios`

## 五、排障

| 现象 | 处理 |
|------|------|
| Android 一直「等待电脑连接」 | 电脑桥接是否运行；电脑蓝牙是否开启；手机是否允许蓝牙/广播权限 |
| iPhone 无通知 | 是否开启「共享系统通知」；config 里 `ancsEnabled` 是否为 true |
| 看板打不开 | `netstat` 看 9875；结束多余 `notify-bridge.exe` 后重启 |
| exe 提示缺少 bleak | 使用 v4 新 exe，或源码 `pip install bleak` |

## 六、WorkBuddy

将 `skill/notify-dashboard` 复制到 `%USERPROFILE%\.workbuddy\skills\`，对话说「通知看板」即可。
